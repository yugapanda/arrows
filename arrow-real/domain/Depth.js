const Point = require("./Point");

module.exports = class Depth {
    /**
     * 
     * @param {Point} point 
     * @param {Number} depth 
     */
    constructor(point, depth) {
        this.point = point;
        this.depth = depth;
    }

    

};